import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apps.dev.tyran.market',
  appName: 'طيران - المطاعم',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
